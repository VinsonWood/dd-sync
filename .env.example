# Docker 用户 UID/GID 配置
# 设置为宿主机用户的 UID/GID，避免文件权限问题
# 获取方法：
#   - UID: id -u
#   - GID: id -g
USER_UID=1001
USER_GID=1001

# TikTokDownloader API 配置
API_BASE_URL=http://192.168.60.20:5555

# 日志级别（DEBUG, INFO, WARN, ERROR）
LOG_LEVEL=INFO
